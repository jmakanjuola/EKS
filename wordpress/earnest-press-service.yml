apiVersion: v1
kind: Service
metadata:
  name: earnest-press-service
spec:
  ports:
  - port: 80
    nodePort: 32001
    targetPort: http-port
    protocol: TCP
  selector:
    app: earnest-press
  type: LoadBalancer
