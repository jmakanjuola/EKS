apiVersion: v1
kind: Service
metadata:
  name: earnest-service
spec:
  ports:
  - port: 31001
    nodePort: 31001
    targetPort: nodejs-port
    protocol: TCP
  selector:
    app: earnest-app
  type: NodePort
